const usuario_model = require('./usuario.model');
const incripcion_model = require('./inscripcion.model');
const avance_model = require('./avance.model');
const proyecto_model = require('./proyecto.model');

module.exports = Object.freeze({
    usuario_model,
    incripcion_model,
    avance_model,
    proyecto_model
});